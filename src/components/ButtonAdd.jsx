
const ButtonAdd = ({openModal}) => {
  return (
    <button onClick={openModal} className="bg-orange-500 hover:bg-orange-600 text-white w-24 rounded-sm">

        + add new
    </button>
  )
}

export default ButtonAdd